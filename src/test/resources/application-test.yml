spring:
  datasource:
    url: jdbc:h2:mem:testdb-${random.uuid};DB_CLOSE_DELAY=-1;MODE=PostgreSQL
  jpa:
    show-sql: false

app:
  reservation:
    window-minutes: 15
    cleanup-interval-ms: 3600000

logging:
  level:
    com.trading: WARN
    org.springframework: WARN
